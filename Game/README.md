# Game Folder Notes

- `index.html`, `style.css`, `main.js` are the active game files.
- Shared assets now come from `../assets/`.
- Old duplicate image files were moved to `legacy-assets/`.
- Previous prototype was renamed to `Final/legacy-final-game/`.
